const array: string[] = [];
const arrayAlt: Array<string> = [];

array.push("javascript");


const numArray: number[] = [];
const numArrayAlt: Array<number> = [];

numArray.push(1);

type UsersObj = {
    name: string,
    email: string
}

const allUsers: UsersObj[] = [];

allUsers.push({name: "", email: ""});


const multiDimArray: number[][] = []

multiDimArray.push([1, 2, 3]);
